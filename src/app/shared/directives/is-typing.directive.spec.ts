import { IsTypingDirective } from './is-typing.directive';

describe('IsTypingDirective', () => {
  it('should create an instance', () => {
    const directive = new IsTypingDirective();
    expect(directive).toBeTruthy();
  });
});
